apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "Output.fullname" . }}-test-connection"
  labels:
{{ include "Output.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "Output.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
