apiVersion: v1
kind: Service
metadata:
  name: {{ include "Output.fullname" . }}
  labels:
    {{- include "Output.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "Output.selectorLabels" . | nindent 4 }}
